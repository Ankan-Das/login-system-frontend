import React from 'react';
import './Check.css';  // Optional, for adding custom styles

const Check = () => {
    return (
        <div className="check-container">
            <h1>Frontend Deployment Test</h1>
            <p>This is a dummy component to verify that the frontend has been deployed successfully.</p>
            <p>If you are seeing this message, your frontend is working!</p>
        </div>
    );
};

export default Check;